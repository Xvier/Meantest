dependencies:
  pre:
   - npm install -g cypress-cli
test:
  pre:
  - npm start:
    background: true
   override:
   - npm test
   - cypress ci
deployment:
  staging:
    branch: master
    heroku:
      appname: nuonstack